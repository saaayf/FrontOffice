<form method="post" action="{{ path('app_entretientf_delete', {'id': entretient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entretient.id) }}">
    <button class="btn">Delete</button>
</form>
