{% extends 'Backbase.html.twig' %}

{% block title %}
    Recrutement
{% endblock %}

{% block body %}
    <h1>Recrutement</h1>
    <br>
    <br>
    <br>
    <br>
    <br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recrutement.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ recrutement.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recrutement.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ recrutement.date ? recrutement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ recrutement.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recrutement_index') }}" class="btn btn-info">Back to List</a>
    <a href="{{ path('app_recrutement_edit', {'id': recrutement.id}) }}" class="btn btn-primary">Edit</a>
    {{ include('recrutement/_delete_form.html.twig') }}
{% endblock %}
