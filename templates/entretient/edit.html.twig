{% extends 'Backbase.html.twig' %}

{% block title %}Edit Entretient{% endblock %}



{% block body %}
    <div class="row">
        <div class="col-md-8 mx-auto">
            <h1 class="display-3 font-weight-bold text-white text-center">Edit Entretient</h1>

            {{ include('entretient/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
    <div class="row pt-4">
        {{ include('entretient/_delete_form.html.twig') }}
        <div class="col-md-12"><a href="{{ path('app_entretient_index') }}" class="btn btn-success"><i class="fa fa-angle-left" aria-hidden="true">&nbsp;</i>
back to list</a></div>
    </div>
{% endblock %}
